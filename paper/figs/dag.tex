% \documentclass[tikz,border=10pt]{standalone}
% \usepackage{tikz}
% \usetikzlibrary{shapes.rounded,arrows.meta,positioning}

% \begin{document}
\begin{tikzpicture}[
  node distance=1.2cm,
  every node/.style={align=center, font=\small},
  dashedbox/.style={rounded corners=8pt, draw, dashed, thick, minimum width=4.5cm, minimum height=1cm, inner sep=8pt},
  solidbox/.style={rounded corners=8pt, draw, thick, minimum width=4.5cm, minimum height=1cm, inner sep=8pt},
  arrow/.style={-{Stealth[length=6pt]}, gray, thick}
]

% Nodes
\node[dashedbox, draw=olive!80] (gen) {Generate Search Combinations};

\node[dashedbox, draw=green!60!black, below left=1.2cm and 0.25cm of gen] (scopus) {Search Scopus};
\node[dashedbox, draw=teal!70, below right=1.2cm and 0.25cm of gen] (wos) {Search Web of Science};

\node[solidbox, draw=green!60!black, below=3.6cm of gen] (merge) {Merge Searches};

\node[solidbox, draw=orange!80, below=1.2cm of merge] (bib) {Generate Bibliography Files};


% Arrows
\draw[arrow] (gen) -- (scopus);
\draw[arrow] (gen) -- (wos);
\draw[arrow] (scopus) -- (merge);
\draw[arrow] (wos) -- (merge);
\draw[arrow] (merge) -- (bib);


\end{tikzpicture}
% \end{document}